import { BoxContainer } from '@ek-components/Container/BoxContainer';
import { styled } from '@mui/material';

export const StyledFormContainer = styled(BoxContainer)({
  flexDirection: 'row',
  alignItems: 'center',
  justifyContent: 'center',
  gap: '10px',
});
